# Enterprise extension hosting (testing only)

See the same README in `v3/internal/enterprise-test/` in the repo — upload your `.crx` here when this folder is part of your deploy.

**Example URL** (if this folder is at site root):  
`https://snapharvest.xyz/internal/enterprise-test/snap-for-harvest-test.crx`

Remove when testing is complete.
